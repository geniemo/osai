# Project 1 Improve — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v1 baseline (75.82% raw / 79.47% TTA) 위에서 4 ablation (Stage 1 160K, Copy-Paste, Class-balanced sampling, Lovász loss) 진행해 mIoU ~+2-3% 추가 → 최종 ~82% TTA.

**Architecture:** `improve` 브랜치, Hybrid 구조 — v2.0 (Stage 1 160K) 한 번 학습 + v2.0 best ckpt를 starting point로 v2.A/B/C 3개 isolated ablation (Stage 2 only) + winning ones를 결합한 v2.final (Stage 1+2 통째 재학습).

**Tech Stack:** PyTorch 2.11+cu128, TorchVision, OpenCV (Copy-Paste resize), pycocotools, WandB, pytest. **3rd party 추가 없음** — 모든 ablation이 우리 codebase + 표준 PyTorch operators만.

**Reference docs:**
- Spec: `docs/superpowers/specs/2026-04-25-p1-improve-design.md`
- v1 spec: `docs/superpowers/specs/2026-04-24-p1-segmentation-design.md`
- Project overview: `docs/p1-overview.md`
- Constraints: `osai/CLAUDE.md`, `p1/CLAUDE.md`

---

## File Structure

### Created files (10)

```
p1/
├── src/
│   ├── config/
│   │   ├── v2_0_stage1_160k.yaml         # v2.0 (Stage 1 160K) — already exists from earlier work
│   │   ├── v2_A_copy_paste.yaml          # v2.A
│   │   ├── v2_B_class_balanced.yaml      # v2.B
│   │   ├── v2_C_lovasz.yaml              # v2.C
│   │   └── v2_final.yaml                 # v2.final (Task 14에서 결과 반영해 작성)
│   ├── data/
│   │   ├── copy_paste.py                 # Copy-Paste augmentation
│   │   └── sampler.py                    # Class-balanced sampling utilities
│   ├── losses/
│   │   └── lovasz.py                     # Lovász-Softmax loss
│   └── compare_runs.py                   # 결과 비교 표 생성
└── tests/
    ├── test_copy_paste.py
    ├── test_sampler.py
    └── test_lovasz.py
```

### Modified files (5)

```
p1/src/data/voc.py          # get_raw + apply_transform 메서드 분리 (Copy-Paste 지원)
p1/src/data/builder.py      # Copy-Paste, Class-balanced sampler 통합
p1/src/losses/seg_loss.py   # lovasz_weight 옵션 추가
p1/src/train.py             # paths.bootstrap_from 지원
p1/src/utils/viz.py         # Copy-Paste 시각화 헬퍼 (선택)
```

### Pre-existing artifact (재사용)

- `p1/src/config/v2_1_stage1_160k.yaml` — 이전 작업에서 생성. Task 1에서 `v2_0_stage1_160k.yaml`로 rename
- `p1/checkpoints_v1_baseline/` — v1 ckpt 백업 (이미 있음)
- `p1/data/voc/`, `p1/data/coco/` — 데이터 (이미 있음)

---

## Phase A — v2.0 시작 + 단순 ablation 구현 (병렬)

v2.0 학습 background로 돌리며 구현 작업. Wall clock 압축.

### Task 1: v2.0 yaml 정리 + 학습 시작 (background)

**Files:**
- Modify: `p1/src/config/v2_1_stage1_160k.yaml` → rename to `v2_0_stage1_160k.yaml`

- [ ] **Step 1: yaml 파일 rename + 내용 확인**

```bash
cd /home/park/workspace/osai
mv p1/src/config/v2_1_stage1_160k.yaml p1/src/config/v2_0_stage1_160k.yaml
```

이 파일은 이미 `improve` 브랜치에 commit되어 있음 (commit `cbb0740`). rename으로 v2.0 명명 통일.

내용에 다음이 있어야 (없으면 추가):
- `training.stage1_iters: 160000`
- `training.stage2_iters: 8000`
- `wandb.run_name_prefix: v2.0`
- `wandb.tags: [desktop, voc+coco, ablation, v2.0, stage1-160k]`
- `paths.ckpt_dir: ./checkpoints/v2.0`
- 그 외 default.yaml과 동일

- [ ] **Step 2: yaml 변경 commit**

```bash
cd /home/park/workspace/osai
git add p1/src/config/v2_0_stage1_160k.yaml
git rm p1/src/config/v2_1_stage1_160k.yaml 2>/dev/null || true
git commit -m "improve: rename v2.1 yaml to v2.0 (naming consistency with spec)"
```

- [ ] **Step 3: 학습 시작 (background, ETA ~11.5h)**

```bash
cd /home/park/workspace/osai/p1
uv run python -m src.train --config src/config/v2_0_stage1_160k.yaml &
echo $! > /tmp/v2_0_pid
```

또는 background process API:
```python
# subagent uses Bash with run_in_background=True
# Bash(command="cd /home/park/workspace/osai/p1 && uv run python -m src.train --config src/config/v2_0_stage1_160k.yaml", run_in_background=True)
```

- [ ] **Step 4: 학습 시작 verification**

```bash
sleep 30
ps -ef | grep "src.train.*v2_0" | grep -v grep | wc -l
```
Expected: 1 (process running)

WandB run URL 확인:
```bash
grep "View run" $(ls -t /tmp/claude*/tasks/*.output | head -1) 2>/dev/null || echo "check WandB web manually"
```

진행 중이면 다음 task로 (병렬). 학습은 background, 다른 작업 가능.

---

### Task 2: Class-balanced sampling 구현 (sampler.py)

**Files:**
- Create: `p1/src/data/sampler.py`
- Test: `p1/tests/test_sampler.py`

- [ ] **Step 1: 테스트 작성 (`p1/tests/test_sampler.py`)**

```python
import numpy as np
import pytest
import torch

from src.data.sampler import compute_class_pixel_counts, compute_image_weights


def _create_fake_voc_data(tmp_path):
    """Create minimal VOC-like structure with 4 fake masks."""
    voc_dir = tmp_path / "VOCdevkit" / "VOC2012" / "SegmentationClass"
    voc_dir.mkdir(parents=True)
    from PIL import Image
    # Image 0: all background (class 0, 100×100 = 10000 px)
    Image.fromarray(np.zeros((100, 100), dtype=np.uint8), mode="L").save(voc_dir / "0.png")
    # Image 1: half bg, half class 1 (person)
    arr1 = np.zeros((100, 100), dtype=np.uint8); arr1[:, 50:] = 1
    Image.fromarray(arr1, mode="L").save(voc_dir / "1.png")
    # Image 2: small chair (class 9), 10x10 = 100 px
    arr2 = np.zeros((100, 100), dtype=np.uint8); arr2[0:10, 0:10] = 9
    Image.fromarray(arr2, mode="L").save(voc_dir / "2.png")
    # Image 3: ignore (255 dominant)
    arr3 = np.full((100, 100), 255, dtype=np.uint8); arr3[0, 0] = 1
    Image.fromarray(arr3, mode="L").save(voc_dir / "3.png")
    return tmp_path, ["0", "1", "2", "3"]


def test_compute_class_pixel_counts(tmp_path):
    voc_root, ids = _create_fake_voc_data(tmp_path)
    counts = compute_class_pixel_counts(str(voc_root), ids)
    # background (class 0): 10000 + 5000 + 9900 + 0 = 24900
    assert counts[0] == 24900
    # class 1 (person): 5000 + 1 = 5001
    assert counts[1] == 5001
    # class 9 (chair): 100
    assert counts[9] == 100
    # class 255 not counted
    assert len(counts) == 21


def test_compute_image_weights_weak_class_higher(tmp_path):
    voc_root, ids = _create_fake_voc_data(tmp_path)
    counts = compute_class_pixel_counts(str(voc_root), ids)
    class_weights = 1.0 / np.sqrt(counts.astype(float) + 1)
    class_weights = class_weights / class_weights.sum()
    image_weights = compute_image_weights(str(voc_root), ids, class_weights)
    # Image 2 (has chair, weak class) should have higher weight than Image 1 (has person, strong class)
    # Both also have background. weight = sum of class_weights for present classes.
    # Image 0: weight ≈ class_weights[0]
    # Image 1: weight ≈ class_weights[0] + class_weights[1]
    # Image 2: weight ≈ class_weights[0] + class_weights[9]
    # Image 3: weight ≈ class_weights[1] (only 1 valid pixel, but present in unique)
    assert image_weights[2] > image_weights[1]   # chair > person


def test_image_weights_skip_ignore(tmp_path):
    voc_root, ids = _create_fake_voc_data(tmp_path)
    counts = compute_class_pixel_counts(str(voc_root), ids)
    class_weights = 1.0 / np.sqrt(counts.astype(float) + 1)
    class_weights = class_weights / class_weights.sum()
    image_weights = compute_image_weights(str(voc_root), ids, class_weights)
    # Image 3 has 255 (ignore) + 1 px of class 1 → only class 1 counted
    # weight = class_weights[1]
    assert abs(image_weights[3].item() - class_weights[1].item()) < 1e-6
```

- [ ] **Step 2: 테스트 fail 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_sampler.py -v
```
Expected: FAIL — `ModuleNotFoundError: No module named 'src.data.sampler'`

- [ ] **Step 3: `p1/src/data/sampler.py` 구현**

```python
"""Class-balanced sampling for VOC train (Stage 2 ablation v2.B).

Inverse sqrt frequency 기반 image weight 계산 → WeightedRandomSampler.
weak class (chair, bicycle, sofa, pottedplant) 포함 이미지 더 자주 sample.
"""
from __future__ import annotations

from pathlib import Path
from typing import List

import numpy as np
import torch
from PIL import Image
from torch.utils.data import WeightedRandomSampler

NUM_CLASSES = 21
IGNORE_INDEX = 255


def compute_class_pixel_counts(voc_root: str, train_ids: List[str]) -> np.ndarray:
    """VOC train 마스크에서 class별 픽셀 수 합계.

    Returns: shape (21,) int64 array. ignore (255) 픽셀은 제외.
    """
    counts = np.zeros(NUM_CLASSES, dtype=np.int64)
    seg_dir = Path(voc_root) / "VOCdevkit" / "VOC2012" / "SegmentationClass"
    for img_id in train_ids:
        mask = np.array(Image.open(seg_dir / f"{img_id}.png"))
        for c in range(NUM_CLASSES):
            counts[c] += int((mask == c).sum())
    return counts


def compute_image_weights(
    voc_root: str,
    train_ids: List[str],
    class_weights: np.ndarray,
) -> torch.Tensor:
    """이미지에 등장하는 class들의 class_weight 합 = 그 이미지의 sampling weight.

    Returns: shape (len(train_ids),) float tensor.
    """
    seg_dir = Path(voc_root) / "VOCdevkit" / "VOC2012" / "SegmentationClass"
    image_weights = []
    for img_id in train_ids:
        mask = np.array(Image.open(seg_dir / f"{img_id}.png"))
        present = np.unique(mask)
        present = present[(present != IGNORE_INDEX) & (present < NUM_CLASSES)]
        w = float(class_weights[present].sum())
        image_weights.append(w)
    return torch.tensor(image_weights, dtype=torch.float32)


def build_balanced_sampler(voc_root: str, train_ids: List[str]) -> WeightedRandomSampler:
    """전체 pipeline: counts → class_weights (inverse sqrt) → image_weights → Sampler."""
    counts = compute_class_pixel_counts(voc_root, train_ids)
    class_weights = 1.0 / np.sqrt(counts.astype(np.float64) + 1.0)
    class_weights = class_weights / class_weights.sum()
    image_weights = compute_image_weights(voc_root, train_ids, class_weights)
    return WeightedRandomSampler(
        weights=image_weights,
        num_samples=len(image_weights),
        replacement=True,
    )
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_sampler.py -v
```
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
cd /home/park/workspace/osai
git add p1/src/data/sampler.py p1/tests/test_sampler.py
git commit -m "improve: add class-balanced sampling (sampler.py + tests)

inverse sqrt class freq → image weight → WeightedRandomSampler.
weak class 포함 이미지 더 자주 sample. Stage 2 (VOC only) 한정 적용 예정.

3 tests pass."
```

---

### Task 3: Lovász-Softmax loss 구현 (lovasz.py)

**Files:**
- Create: `p1/src/losses/lovasz.py`
- Test: `p1/tests/test_lovasz.py`

- [ ] **Step 1: 테스트 작성 (`p1/tests/test_lovasz.py`)**

```python
import torch

from src.losses.lovasz import lovasz_grad, lovasz_softmax


def test_lovasz_grad_known_input():
    """Lovász gradient on toy input."""
    # gt_sorted = [1, 1, 0, 0]: 2 positives sorted first
    gt = torch.tensor([1.0, 1.0, 0.0, 0.0])
    grad = lovasz_grad(gt)
    # Manual computation:
    # gts = 2; intersection = 2 - cumsum([1,1,0,0]) = [1,0,0,0]
    # union = 2 + cumsum([0,0,1,1]) = [2,2,3,4]
    # jaccard = 1 - intersection/union = [0.5, 1.0, 1.0, 1.0]
    # diff (i>=1): [0.5, 0.5, 0, 0]
    expected = torch.tensor([0.5, 0.5, 0.0, 0.0])
    assert torch.allclose(grad, expected, atol=1e-6)


def test_lovasz_softmax_perfect_prediction():
    """Perfect logits → loss ≈ 0."""
    N, C, H, W = 1, 21, 16, 16
    target = torch.randint(0, C, (N, H, W))
    logits = torch.zeros(N, C, H, W)
    # very high logit on correct class → softmax ≈ 1 there
    logits.scatter_(1, target.unsqueeze(1), 100.0)
    loss = lovasz_softmax(logits, target)
    assert loss.item() < 0.01


def test_lovasz_softmax_random_in_range():
    """Random logits → loss in (0, 1)."""
    N, C, H, W = 2, 21, 32, 32
    logits = torch.randn(N, C, H, W)
    target = torch.randint(0, C, (N, H, W))
    loss = lovasz_softmax(logits, target)
    assert 0.0 < loss.item() < 1.0


def test_lovasz_softmax_ignore_index():
    """Pixels with ignore_index=255 should not contribute."""
    N, C, H, W = 1, 21, 8, 8
    logits = torch.randn(N, C, H, W)
    target_valid = torch.randint(0, C, (N, H, W))
    loss_valid = lovasz_softmax(logits, target_valid, ignore_index=255)

    # All pixels ignore → loss should be 0 (no class contributes)
    target_all_ignore = torch.full((N, H, W), 255)
    loss_ignored = lovasz_softmax(logits, target_all_ignore, ignore_index=255)
    assert loss_ignored.item() == 0.0


def test_lovasz_softmax_gradient_flow():
    """Backward pass works without NaN."""
    N, C, H, W = 1, 21, 8, 8
    logits = torch.randn(N, C, H, W, requires_grad=True)
    target = torch.randint(0, C, (N, H, W))
    loss = lovasz_softmax(logits, target)
    loss.backward()
    assert logits.grad is not None
    assert torch.isfinite(logits.grad).all()
```

- [ ] **Step 2: 테스트 fail 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_lovasz.py -v
```
Expected: FAIL — module not found

- [ ] **Step 3: `p1/src/losses/lovasz.py` 구현**

```python
"""Lovász-Softmax loss (Berman et al. 2018).

mIoU의 differentiable smooth surrogate. Cross-Entropy와 결합해 IoU를 직접
최적화. Reference: https://arxiv.org/abs/1705.08790
"""
from __future__ import annotations

import torch
import torch.nn.functional as F


def lovasz_grad(gt_sorted: torch.Tensor) -> torch.Tensor:
    """Lovász extension gradient (paper Algorithm 1).

    Args:
        gt_sorted: 1D tensor, ground truth sorted by descending error.

    Returns:
        gradient tensor same shape as gt_sorted.
    """
    p = len(gt_sorted)
    gts = gt_sorted.sum()
    intersection = gts - gt_sorted.float().cumsum(0)
    union = gts + (1 - gt_sorted).float().cumsum(0)
    jaccard = 1.0 - intersection / union
    if p > 1:
        jaccard[1:p] = jaccard[1:p] - jaccard[0:-1]
    return jaccard


def lovasz_softmax_flat(
    probs: torch.Tensor,
    labels: torch.Tensor,
    classes: str = "present",
    ignore_index: int = 255,
) -> torch.Tensor:
    """Lovász-Softmax on flattened (P, C) probs + (P,) labels.

    classes: 'all' (all C classes) or 'present' (only labels actually appearing).
    """
    valid = labels != ignore_index
    probs = probs[valid]
    labels = labels[valid]
    if probs.numel() == 0:
        return probs.sum() * 0.0  # 0 with grad

    losses = []
    if classes == "present":
        class_set = torch.unique(labels).tolist()
    else:
        class_set = list(range(probs.size(1)))

    for c in class_set:
        if c >= probs.size(1):
            continue
        fg = (labels == c).float()  # (P,) binary
        if classes == "present" and fg.sum() == 0:
            continue
        errors = (fg - probs[:, c]).abs()
        errors_sorted, perm = torch.sort(errors, descending=True)
        fg_sorted = fg[perm]
        losses.append(torch.dot(errors_sorted, lovasz_grad(fg_sorted)))

    if not losses:
        return probs.sum() * 0.0
    return torch.stack(losses).mean()


def lovasz_softmax(
    logits: torch.Tensor,
    labels: torch.Tensor,
    ignore_index: int = 255,
) -> torch.Tensor:
    """Lovász-Softmax loss for segmentation.

    Args:
        logits: (N, C, H, W) raw logits (pre-softmax).
        labels: (N, H, W) long, class IDs in [0, C-1] or ignore_index.
        ignore_index: pixel value to skip.

    Returns:
        scalar loss.
    """
    probs = F.softmax(logits, dim=1)
    N, C, H, W = probs.shape
    probs = probs.permute(0, 2, 3, 1).reshape(-1, C)
    labels = labels.reshape(-1)
    return lovasz_softmax_flat(probs, labels, classes="present", ignore_index=ignore_index)
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_lovasz.py -v
```
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
cd /home/park/workspace/osai
git add p1/src/losses/lovasz.py p1/tests/test_lovasz.py
git commit -m "improve: add Lovász-Softmax loss (lovasz.py + tests)

mIoU의 differentiable surrogate. CE와 결합 또는 Dice 교체로 사용.
Reference: Berman et al. 2018.

5 tests pass: lovasz_grad formula 검증, perfect prediction, random,
ignore_index 처리, gradient flow."
```

---

### Task 4: SegLoss에 lovasz_weight 옵션 추가

**Files:**
- Modify: `p1/src/losses/seg_loss.py`
- Modify: `p1/tests/test_loss.py` (extend existing tests)

- [ ] **Step 1: 기존 test_loss.py에 Lovász test 추가**

`p1/tests/test_loss.py` 끝에 다음 추가:

```python
def test_lovasz_weight_active():
    """lovasz_weight > 0 → loss includes Lovász term."""
    crit = SegLoss(num_classes=2, ignore_index=255,
                   dice_weight=0.0, lovasz_weight=0.5, aux_weight=0.0)
    target = torch.randint(0, 2, (1, 4, 4))
    main = torch.randn(1, 2, 4, 4)
    loss = crit(main, None, target)
    assert torch.isfinite(loss)


def test_lovasz_replaces_dice_independent():
    """dice_weight=0 + lovasz_weight=0.5 → only CE+Lovász."""
    target = torch.randint(0, 2, (1, 4, 4))
    main = torch.randn(1, 2, 4, 4)
    # CE-only
    crit_ce = SegLoss(num_classes=2, dice_weight=0.0, lovasz_weight=0.0, aux_weight=0.0)
    loss_ce = crit_ce(main, None, target)
    # CE + Lovász
    crit_lov = SegLoss(num_classes=2, dice_weight=0.0, lovasz_weight=0.5, aux_weight=0.0)
    loss_lov = crit_lov(main, None, target)
    # Lovász term should change loss (almost always different from CE alone)
    assert abs(loss_lov.item() - loss_ce.item()) > 1e-4
```

- [ ] **Step 2: `p1/src/losses/seg_loss.py` 수정**

`SegLoss.__init__`에 `lovasz_weight=0.0` 인자 추가:

```python
class SegLoss(nn.Module):
    def __init__(
        self,
        num_classes: int = 21,
        ignore_index: int = 255,
        dice_weight: float = 0.5,
        lovasz_weight: float = 0.0,    # 새 인자
        aux_weight: float = 0.4,
    ) -> None:
        super().__init__()
        self.ce = nn.CrossEntropyLoss(ignore_index=ignore_index, reduction="sum")
        self.ignore_index = ignore_index
        self.num_classes = num_classes
        self.dice = DiceLoss(num_classes=num_classes, ignore_index=ignore_index)
        self.dice_weight = dice_weight
        self.lovasz_weight = lovasz_weight    # 저장
        self.aux_weight = aux_weight

    def _ce_safe(self, logits, target):
        """Sum reduction + manual normalize (NaN 방지 when all ignore)."""
        loss = self.ce(logits, target)
        n_valid = (target != self.ignore_index).sum().clamp(min=1)
        return loss / n_valid

    def forward(self, main_logits, aux_logits, target):
        loss = self._ce_safe(main_logits, target)
        if self.dice_weight > 0:
            loss = loss + self.dice_weight * self.dice(main_logits, target)
        if self.lovasz_weight > 0:
            from src.losses.lovasz import lovasz_softmax
            loss = loss + self.lovasz_weight * lovasz_softmax(
                main_logits, target, ignore_index=self.ignore_index
            )
        if aux_logits is not None and self.aux_weight > 0:
            loss = loss + self.aux_weight * self._ce_safe(aux_logits, target)
        return loss
```

(주의: 기존 `_ce_safe` 메서드와 reduction="sum" 처리가 이미 있을 수 있음. 코드 검토 후 일관 유지.)

- [ ] **Step 3: 모든 loss test 통과 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_loss.py -v
```
Expected: 5 passed (3 기존 + 2 신규)

- [ ] **Step 4: Commit**

```bash
cd /home/park/workspace/osai
git add p1/src/losses/seg_loss.py p1/tests/test_loss.py
git commit -m "improve: SegLoss에 lovasz_weight 옵션 추가

기존 dice_weight + 새 lovasz_weight 동시 지원.
v2.C는 dice_weight=0.0 + lovasz_weight=0.5로 사용 (Dice 교체).

기존 3 tests + 2 신규 tests = 5 passed."
```

---

### Task 5: train.py에 bootstrap_from 메커니즘 추가

**Files:**
- Modify: `p1/src/train.py`

- [ ] **Step 1: train.py의 `run_stage()` 함수에 bootstrap 로직 추가**

기존 코드에서 stage 2 시작 시 best_ckpt 로드하는 부분을 찾아 그 앞에 bootstrap_from 우선 적용:

기존:
```python
elif stage == 2 and Path(cfg["paths"]["best_ckpt"]).exists():
    bc = torch.load(cfg["paths"]["best_ckpt"], map_location="cpu", weights_only=False)
    model.load_state_dict(bc["ema_state"])
    ema_model.load_state_dict(bc["ema_state"])
    print(f"[stage2] loaded best ckpt from stage1 (mIoU={bc['miou']:.4f})")
```

수정 후:
```python
elif stage == 2:
    bootstrap_path = cfg["paths"].get("bootstrap_from")
    if bootstrap_path and Path(bootstrap_path).exists():
        bc = torch.load(bootstrap_path, map_location="cpu", weights_only=False)
        state = bc.get("ema_state", bc)
        model.load_state_dict(state)
        ema_model.load_state_dict(state)
        miou = bc.get("miou", "unknown")
        print(f"[bootstrap] from {bootstrap_path} (mIoU={miou})")
    elif Path(cfg["paths"]["best_ckpt"]).exists():
        bc = torch.load(cfg["paths"]["best_ckpt"], map_location="cpu", weights_only=False)
        model.load_state_dict(bc["ema_state"])
        ema_model.load_state_dict(bc["ema_state"])
        print(f"[stage2] loaded best ckpt from stage1 (mIoU={bc['miou']:.4f})")
```

핵심:
- yaml에 `paths.bootstrap_from`이 있고 파일 존재하면 그것 우선
- 없으면 기존 동작 (같은 ckpt_dir의 best_ckpt 로드)

- [ ] **Step 2: 변경 검증 (import + 호출 가능)**

```bash
cd /home/park/workspace/osai/p1 && uv run python -c "import src.train; print('train.py OK')"
```
Expected: `train.py OK`

- [ ] **Step 3: Commit**

```bash
cd /home/park/workspace/osai
git add p1/src/train.py
git commit -m "improve: train.py에 paths.bootstrap_from 지원

ablation 학습 (v2.A/B/C)이 v2.0 best ckpt에서 시작할 수 있도록.
bootstrap_from > best_ckpt 우선순위 (없으면 기존 동작 그대로)."
```

---

## Phase B — Copy-Paste 구현 (가장 큰 작업)

v2.0 학습 끝났는지 확인 후 진행. v2.0 결과도 측정.

### Task 6: v2.0 학습 완료 대기 + eval

**Files:** (verification only)

- [ ] **Step 1: v2.0 학습 완료 확인**

학습 process가 끝났는지 확인 (background process notification 또는 ckpt 파일 timestamp):
```bash
ls -la /home/park/workspace/osai/p1/checkpoints/v2.0/
```
Expected: `model.pth`, `best.pth`, `training_state_stage{1,2}.pth` 모두 존재

만약 아직 실행 중이면 완료까지 대기. 완료 알림이 background task에서 옴.

- [ ] **Step 2: v2.0 raw eval**

```bash
cd /home/park/workspace/osai/p1
uv run python -m src.eval --config src/config/v2_0_stage1_160k.yaml --ckpt checkpoints/v2.0/best.pth 2>&1 | tee /tmp/v2_0_eval.log
```
Expected: `mIoU: 0.7XXX` (예상 0.76~0.78), per-class IoU 21개

- [ ] **Step 3: v2.0 TTA eval**

```bash
cd /home/park/workspace/osai/p1
uv run python -m src.eval_tta --config src/config/v2_0_stage1_160k.yaml --ckpt checkpoints/v2.0/best.pth 2>&1 | tee /tmp/v2_0_tta.log
```
Expected: `TTA mIoU: 0.8XXX` (예상 0.80~0.82)

- [ ] **Step 4: 결과 기록 (commit message에)**

```bash
cd /home/park/workspace/osai
git commit --allow-empty -m "result: v2.0 (Stage 1 160K) measured

raw mIoU: $(grep '^mIoU' /tmp/v2_0_eval.log | head -1)
TTA mIoU: $(grep '^TTA mIoU' /tmp/v2_0_tta.log | head -1)
Δ vs v1: TBD (계산 후 다음 commit에서)

WandB run: (URL from start)
"
```

(empty commit으로 결과 timeline 기록. 또는 README에 기록.)

---

### Task 7: VOCSegDataset refactor (raw + transform 분리)

**Files:**
- Modify: `p1/src/data/voc.py`

Copy-Paste가 transform 전 raw PIL 이미지에 접근해야 하므로 분리.

- [ ] **Step 1: 기존 voc.py 읽고 구조 확인**

```bash
cat /home/park/workspace/osai/p1/src/data/voc.py
```

현재 `__getitem__`이 (raw load + transform)을 한 번에 함. 이걸 두 메서드로 분리.

- [ ] **Step 2: voc.py 수정 (`get_raw` + `apply_transform` 추가, `__getitem__`은 둘을 호출)**

```python
"""Pascal-VOC 2012 segmentation dataset (train + val)."""
from __future__ import annotations

from pathlib import Path
from typing import Callable, Optional, Tuple

from PIL import Image
from torch.utils.data import Dataset


class VOCSegDataset(Dataset):
    def __init__(
        self,
        root: str,
        split: str,
        transform: Optional[Callable] = None,
    ) -> None:
        self.root = Path(root)
        self.split = split
        self.transform = transform
        self.devkit = self.root / "VOCdevkit" / "VOC2012"

        if split == "train":
            id_file = self.devkit / "ImageSets" / "Segmentation" / "train.txt"
        elif split == "val":
            id_file = self.devkit / "ImageSets" / "Segmentation" / "val.txt"
        else:
            raise ValueError(f"Unknown split: {split} (allowed: 'train', 'val')")

        mask_dir = self.devkit / "SegmentationClass"

        with open(id_file) as f:
            self.ids = [line.strip() for line in f if line.strip()]
        self.img_dir = self.devkit / "JPEGImages"
        self.mask_dir = mask_dir

    def __len__(self) -> int:
        return len(self.ids)

    def get_raw(self, index: int) -> Tuple[Image.Image, Image.Image]:
        """Return raw PIL (image, mask) without transform.

        Used by Copy-Paste wrapper to manipulate before applying transforms.
        """
        img_id = self.ids[index]
        img = Image.open(self.img_dir / f"{img_id}.jpg").convert("RGB")
        mask = Image.open(self.mask_dir / f"{img_id}.png")
        return img, mask

    def apply_transform(self, img: Image.Image, mask: Image.Image):
        """Apply self.transform to (img, mask) tuple."""
        if self.transform is not None:
            return self.transform(img, mask)
        return img, mask

    def __getitem__(self, index: int):
        img, mask = self.get_raw(index)
        return self.apply_transform(img, mask)
```

- [ ] **Step 3: 기존 voc test 통과 확인 (regression 없음)**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_voc.py -v
```
Expected: 1 passed (또는 skipped if no VOC data; 우리는 data 있음)

- [ ] **Step 4: 전체 test suite 통과 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/ -v 2>&1 | tail -3
```
Expected: 모든 기존 test pass (regression 없음)

- [ ] **Step 5: Commit**

```bash
cd /home/park/workspace/osai
git add p1/src/data/voc.py
git commit -m "improve: refactor VOCSegDataset with get_raw + apply_transform

Copy-Paste wrapper가 transform 전 raw PIL에 접근할 수 있도록 분리.
__getitem__은 두 메서드를 호출 (기존 동작 동일).

기존 tests 모두 pass."
```

---

### Task 8: copy_paste.py — instance pool 추출

**Files:**
- Create: `p1/src/data/copy_paste.py` (instance pool part)
- Test: `p1/tests/test_copy_paste.py` (instance pool tests)

- [ ] **Step 1: 테스트 작성 (instance pool 추출 부분)**

`p1/tests/test_copy_paste.py`:

```python
import os
from pathlib import Path

import numpy as np
import pytest
from PIL import Image

from src.data.copy_paste import build_instance_pool


VOC_ROOT = Path(os.environ.get("VOC_ROOT", "data/voc"))
HAS_VOC = (VOC_ROOT / "VOCdevkit/VOC2012/SegmentationObject").exists()


def _make_synthetic_voc(tmp_path):
    """Create minimal VOC-like structure with 1 image + 2 instances."""
    devkit = tmp_path / "VOCdevkit" / "VOC2012"
    (devkit / "JPEGImages").mkdir(parents=True)
    (devkit / "SegmentationClass").mkdir(parents=True)
    (devkit / "SegmentationObject").mkdir(parents=True)

    # 1 fake image: 200×200 RGB
    img = np.random.randint(0, 255, (200, 200, 3), dtype=np.uint8)
    Image.fromarray(img).save(devkit / "JPEGImages" / "fake1.jpg")

    # SegmentationClass: instance 1 = class 8 (cat), instance 2 = class 12 (dog)
    seg_cls = np.zeros((200, 200), dtype=np.uint8)
    seg_cls[20:80, 20:80] = 8     # cat region
    seg_cls[100:160, 100:160] = 12  # dog region
    Image.fromarray(seg_cls, mode="L").save(devkit / "SegmentationClass" / "fake1.png")

    # SegmentationObject: instance IDs (1 and 2)
    seg_obj = np.zeros((200, 200), dtype=np.uint8)
    seg_obj[20:80, 20:80] = 1
    seg_obj[100:160, 100:160] = 2
    Image.fromarray(seg_obj, mode="L").save(devkit / "SegmentationObject" / "fake1.png")

    return tmp_path, ["fake1"]


def test_build_instance_pool_returns_correct_count(tmp_path):
    voc_root, ids = _make_synthetic_voc(tmp_path)
    pool = build_instance_pool(str(voc_root), ids, min_area=100, max_area_ratio=0.99)
    # 60×60 = 3600 px each (above min_area 100, below max 200×200×0.99)
    assert len(pool) == 2
    classes = sorted([cls for _, _, cls in pool])
    assert classes == [8, 12]


def test_build_instance_pool_respects_min_area(tmp_path):
    voc_root, ids = _make_synthetic_voc(tmp_path)
    # min_area higher than instance size (3600) → filter out
    pool = build_instance_pool(str(voc_root), ids, min_area=4096, max_area_ratio=0.99)
    assert len(pool) == 0


def test_build_instance_pool_respects_max_area(tmp_path):
    voc_root, ids = _make_synthetic_voc(tmp_path)
    # max_area_ratio: 200×200 × 0.05 = 2000, instance = 3600 → exceeds
    pool = build_instance_pool(str(voc_root), ids, min_area=100, max_area_ratio=0.05)
    assert len(pool) == 0


def test_build_instance_pool_patch_shape(tmp_path):
    voc_root, ids = _make_synthetic_voc(tmp_path)
    pool = build_instance_pool(str(voc_root), ids, min_area=100, max_area_ratio=0.99)
    for patch_img, patch_mask, cls in pool:
        # patch is RGB
        assert patch_img.ndim == 3 and patch_img.shape[2] == 3
        # mask matches patch HxW
        assert patch_mask.shape == patch_img.shape[:2]
        # mask is bool
        assert patch_mask.dtype == bool
        # class is int 1-20
        assert 1 <= cls <= 20


@pytest.mark.skipif(not HAS_VOC, reason="VOC SegmentationObject not present")
def test_build_instance_pool_real_voc():
    """Real VOC train: should return many instances."""
    train_ids_file = VOC_ROOT / "VOCdevkit/VOC2012/ImageSets/Segmentation/train.txt"
    with open(train_ids_file) as f:
        train_ids = [line.strip() for line in f][:50]  # subset for speed
    pool = build_instance_pool(str(VOC_ROOT), train_ids)
    assert len(pool) > 50  # 50 images, expect 100+ instances
    # all classes 1-20
    for _, _, cls in pool:
        assert 1 <= cls <= 20
```

- [ ] **Step 2: 테스트 fail 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_copy_paste.py -v
```
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: `p1/src/data/copy_paste.py` 작성 (instance pool 부분)**

```python
"""Copy-Paste augmentation for segmentation (Ghiasi et al. 2021).

VOC SegmentationObject로 instance 추출 → 다른 학습 이미지 위에 paste.
Stage 2 v2.A ablation에 사용.
"""
from __future__ import annotations

import random
from pathlib import Path
from typing import List, Tuple

import cv2
import numpy as np
from PIL import Image
from torch.utils.data import Dataset


def build_instance_pool(
    voc_root: str,
    train_ids: List[str],
    min_area: int = 64 * 64,
    max_area_ratio: float = 0.25,
) -> List[Tuple[np.ndarray, np.ndarray, int]]:
    """VOC train의 SegmentationObject에서 모든 instance 추출.

    Args:
        voc_root: e.g. "./data/voc"
        train_ids: VOC train image IDs
        min_area: instance 최소 픽셀 수
        max_area_ratio: 이미지 대비 instance 최대 비율 (0~1)

    Returns:
        list of (patch_img: HxWx3 uint8, patch_mask: HxW bool, class_id: int).
    """
    devkit = Path(voc_root) / "VOCdevkit" / "VOC2012"
    seg_cls_dir = devkit / "SegmentationClass"
    seg_obj_dir = devkit / "SegmentationObject"
    img_dir = devkit / "JPEGImages"

    pool = []
    for img_id in train_ids:
        seg_obj_path = seg_obj_dir / f"{img_id}.png"
        if not seg_obj_path.exists():
            continue
        seg_obj = np.array(Image.open(seg_obj_path))
        seg_cls = np.array(Image.open(seg_cls_dir / f"{img_id}.png"))
        img = np.array(Image.open(img_dir / f"{img_id}.jpg").convert("RGB"))
        H, W = seg_obj.shape

        for inst_id in np.unique(seg_obj):
            if inst_id == 0 or inst_id == 255:  # background or boundary
                continue
            inst_mask = seg_obj == inst_id
            area = int(inst_mask.sum())
            if area < min_area or area > int(H * W * max_area_ratio):
                continue
            cls = int(seg_cls[inst_mask][0])
            if cls == 0 or cls == 255:
                continue
            ys, xs = np.where(inst_mask)
            y0, y1 = int(ys.min()), int(ys.max()) + 1
            x0, x1 = int(xs.min()), int(xs.max()) + 1
            patch_img = img[y0:y1, x0:x1].copy()
            patch_mask = inst_mask[y0:y1, x0:x1].copy()
            pool.append((patch_img, patch_mask, cls))

    return pool
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_copy_paste.py -v
```
Expected: 5 passed (4 synthetic + 1 real VOC if data present)

- [ ] **Step 5: Commit**

```bash
cd /home/park/workspace/osai
git add p1/src/data/copy_paste.py p1/tests/test_copy_paste.py
git commit -m "improve: Copy-Paste — build_instance_pool (instance 추출)

VOC SegmentationObject + SegmentationClass 활용해 instance pool 구축.
min_area / max_area_ratio filter로 너무 작거나 큰 instance 제외.

5 tests pass (synthetic + real VOC subset)."
```

---

### Task 9: copy_paste.py — paste_instance + CopyPasteDataset

**Files:**
- Modify: `p1/src/data/copy_paste.py` (add paste_instance + CopyPasteDataset)
- Modify: `p1/tests/test_copy_paste.py` (add tests)

- [ ] **Step 1: paste_instance + CopyPasteDataset test 추가**

`p1/tests/test_copy_paste.py` 끝에:

```python
from src.data.copy_paste import paste_instance, CopyPasteDataset


def test_paste_instance_modifies_target():
    target_img = np.full((100, 100, 3), 255, dtype=np.uint8)  # white image
    target_mask = np.zeros((100, 100), dtype=np.uint8)         # all background

    patch_img = np.zeros((20, 20, 3), dtype=np.uint8)          # black patch
    patch_mask = np.ones((20, 20), dtype=bool)                  # full mask
    cls_id = 8                                                   # cat

    np.random.seed(0)
    random.seed(0)
    out_img, out_mask = paste_instance(
        target_img.copy(), target_mask.copy(), patch_img, patch_mask, cls_id
    )
    # 적어도 일부 픽셀이 0으로 변경됨 (patch 색상)
    assert (out_img == 0).any()
    # 적어도 일부 픽셀이 cls_id로 변경됨
    assert (out_mask == cls_id).any()
    # paste된 면적은 정확히 patch 면적과 같아야 (resize 없을 때)
    assert (out_mask == cls_id).sum() == 20 * 20


def test_paste_instance_resizes_too_large_patch():
    """patch가 target의 50%보다 크면 자동 resize."""
    target_img = np.full((100, 100, 3), 255, dtype=np.uint8)
    target_mask = np.zeros((100, 100), dtype=np.uint8)
    # huge patch (90×90, > 50% of target)
    patch_img = np.zeros((90, 90, 3), dtype=np.uint8)
    patch_mask = np.ones((90, 90), dtype=bool)

    np.random.seed(0)
    random.seed(0)
    out_img, out_mask = paste_instance(
        target_img.copy(), target_mask.copy(), patch_img, patch_mask, 8
    )
    # paste된 영역은 50×50 이하 (resize됨)
    assert (out_mask == 8).sum() <= 50 * 50


def test_copy_paste_dataset_preserves_length(tmp_path):
    """CopyPasteDataset wrap은 base length 유지."""
    from src.data.voc import VOCSegDataset
    voc_root, ids = _make_synthetic_voc(tmp_path)
    base = VOCSegDataset(root=str(voc_root), split="train", transform=None)
    # train.txt도 만들어야
    seg_dir = voc_root / "VOCdevkit/VOC2012/ImageSets/Segmentation"
    seg_dir.mkdir(parents=True, exist_ok=True)
    (seg_dir / "train.txt").write_text("\n".join(ids))
    base = VOCSegDataset(root=str(voc_root), split="train", transform=None)

    pool = build_instance_pool(str(voc_root), ids, min_area=100, max_area_ratio=0.99)
    cp_ds = CopyPasteDataset(base, pool, p=1.0, num_paste=(1, 2))
    assert len(cp_ds) == len(base)
```

- [ ] **Step 2: 테스트 fail 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_copy_paste.py -v
```
Expected: 새 3 tests fail (이전 5는 pass)

- [ ] **Step 3: `p1/src/data/copy_paste.py`에 paste_instance + CopyPasteDataset 추가**

기존 파일 끝에 추가:

```python
def paste_instance(
    target_img: np.ndarray,
    target_mask: np.ndarray,
    patch_img: np.ndarray,
    patch_mask: np.ndarray,
    cls_id: int,
) -> Tuple[np.ndarray, np.ndarray]:
    """Paste single instance onto target. In-place modification."""
    Ht, Wt = target_img.shape[:2]
    Hp, Wp = patch_img.shape[:2]

    # patch가 target의 50% 이상이면 random scale down
    if Hp > Ht * 0.5 or Wp > Wt * 0.5:
        scale = min(Ht * 0.5 / Hp, Wt * 0.5 / Wp)
        new_H, new_W = max(1, int(Hp * scale)), max(1, int(Wp * scale))
        patch_img = cv2.resize(patch_img, (new_W, new_H), interpolation=cv2.INTER_LINEAR)
        patch_mask = cv2.resize(
            patch_mask.astype(np.uint8), (new_W, new_H), interpolation=cv2.INTER_NEAREST
        ).astype(bool)
        Hp, Wp = new_H, new_W

    # random hflip
    if random.random() < 0.5:
        patch_img = patch_img[:, ::-1].copy()
        patch_mask = patch_mask[:, ::-1].copy()

    # random center position (patch가 target에 들어가도록)
    if Ht <= Hp or Wt <= Wp:
        # patch가 여전히 더 크면 (edge case after resize), skip paste
        return target_img, target_mask
    cy = random.randint(Hp // 2, Ht - Hp + Hp // 2)
    cx = random.randint(Wp // 2, Wt - Wp + Wp // 2)
    y0, y1 = cy - Hp // 2, cy - Hp // 2 + Hp
    x0, x1 = cx - Wp // 2, cx - Wp // 2 + Wp

    # bounds clip (안전망)
    y0, y1 = max(0, y0), min(Ht, y1)
    x0, x1 = max(0, x0), min(Wt, x1)
    Hp_clip, Wp_clip = y1 - y0, x1 - x0
    patch_img = patch_img[:Hp_clip, :Wp_clip]
    patch_mask = patch_mask[:Hp_clip, :Wp_clip]

    # paste
    region = target_img[y0:y1, x0:x1]
    region[patch_mask] = patch_img[patch_mask]
    target_img[y0:y1, x0:x1] = region
    target_mask[y0:y1, x0:x1][patch_mask] = cls_id

    return target_img, target_mask


class CopyPasteDataset(Dataset):
    """Wraps VOCSegDataset; with prob p, paste 1-N instances from pool."""

    def __init__(
        self,
        base: Dataset,
        instance_pool: List[Tuple[np.ndarray, np.ndarray, int]],
        p: float = 0.5,
        num_paste: Tuple[int, int] = (1, 3),
    ) -> None:
        self.base = base
        self.pool = instance_pool
        self.p = p
        self.num_paste_range = num_paste

    def __len__(self) -> int:
        return len(self.base)

    def __getitem__(self, idx: int):
        img_pil, mask_pil = self.base.get_raw(idx)
        if random.random() < self.p and len(self.pool) > 0:
            img_arr = np.array(img_pil)
            mask_arr = np.array(mask_pil)
            n = random.randint(*self.num_paste_range)
            for _ in range(n):
                patch_img, patch_mask, cls_id = random.choice(self.pool)
                img_arr, mask_arr = paste_instance(
                    img_arr, mask_arr, patch_img, patch_mask, cls_id
                )
            img_pil = Image.fromarray(img_arr)
            mask_pil = Image.fromarray(mask_arr)
        return self.base.apply_transform(img_pil, mask_pil)
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_copy_paste.py -v
```
Expected: 8 passed (5 + 3)

- [ ] **Step 5: Commit**

```bash
cd /home/park/workspace/osai
git add p1/src/data/copy_paste.py p1/tests/test_copy_paste.py
git commit -m "improve: Copy-Paste — paste_instance + CopyPasteDataset wrapper

paste_instance: hard paste with auto resize (target 50% 초과 시) + random hflip.
CopyPasteDataset: VOCSegDataset wrap, prob p=0.5로 1-N instance paste.

8 tests pass total."
```

---

### Task 10: builder.py에 Copy-Paste + Class-balanced 통합

**Files:**
- Modify: `p1/src/data/builder.py`

- [ ] **Step 1: builder.py 수정**

기존 `build_dataloaders` 함수에 두 옵션 추가:

```python
"""DataLoader builder + test set 격리 가드 (개발 안전망)."""
from __future__ import annotations

from pathlib import Path, PurePath
from typing import Tuple

from torch.utils.data import ConcatDataset, DataLoader

from src.data.coco import COCOSegDataset
from src.data.transforms import build_train_transform, build_val_transform
from src.data.voc import VOCSegDataset
from src.utils.seed import worker_init_fn


def _assert_not_test_path(path: str, *, role: str) -> None:
    parts = PurePath(path).parts
    assert "input" not in parts, (
        f"{role}={path!r} must not include 'input/'"
    )


def build_dataloaders(cfg: dict) -> Tuple[DataLoader, DataLoader]:
    data_cfg = cfg["data"]
    voc_root = data_cfg["voc_root"]
    coco_root = data_cfg["coco_root"]
    _assert_not_test_path(voc_root, role="voc_root")
    _assert_not_test_path(coco_root, role="coco_root")

    train_t = build_train_transform(
        crop_size=data_cfg["crop_size"],
        scale_range=tuple(data_cfg["scale_range"]),
    )
    val_t = build_val_transform()

    voc_train = VOCSegDataset(root=voc_root, split="train", transform=train_t)
    voc_val = VOCSegDataset(root=voc_root, split="val", transform=val_t)

    stage = data_cfg.get("stage", 1)
    if stage == 1:
        coco_train = COCOSegDataset(coco_root=coco_root, split="train2017", transform=train_t)
        train_ds = ConcatDataset([coco_train, voc_train])
    else:
        train_ds = voc_train

    # Copy-Paste (Stage 2 한정 활성화 — Stage 1은 ConcatDataset이라 wrap 어려움)
    cp_cfg = data_cfg.get("copy_paste", {})
    if cp_cfg.get("enabled", False) and stage == 2:
        from src.data.copy_paste import build_instance_pool, CopyPasteDataset
        train_ids = voc_train.ids
        print(f"[copy-paste] building instance pool from {len(train_ids)} VOC images...")
        pool = build_instance_pool(voc_root, train_ids)
        print(f"[copy-paste] pool size: {len(pool)} instances")
        train_ds = CopyPasteDataset(
            train_ds,
            pool,
            p=cp_cfg.get("p", 0.5),
            num_paste=tuple(cp_cfg.get("num_paste", [1, 3])),
        )

    # Class-balanced sampling (Stage 2 한정)
    use_sampler = data_cfg.get("class_balanced", False) and stage == 2
    if use_sampler:
        from src.data.sampler import build_balanced_sampler
        sampler = build_balanced_sampler(voc_root, voc_train.ids)
        shuffle = False
        print(f"[class-balanced] WeightedRandomSampler activated for Stage 2")
    else:
        sampler = None
        shuffle = True

    common = dict(
        num_workers=data_cfg["num_workers"],
        pin_memory=data_cfg["pin_memory"],
        persistent_workers=data_cfg.get("persistent_workers", True),
        worker_init_fn=worker_init_fn,
    )
    train_loader = DataLoader(
        train_ds,
        batch_size=data_cfg["batch_size"],
        shuffle=shuffle,
        drop_last=True,
        sampler=sampler,
        **common,
    )
    val_loader = DataLoader(voc_val, batch_size=1, shuffle=False, drop_last=False, **common)
    return train_loader, val_loader
```

- [ ] **Step 2: 기존 builder test pass 확인 (regression 없음)**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/test_data_builder.py -v
```
Expected: 3 passed (격리 가드 tests 그대로)

- [ ] **Step 3: 전체 test 통과 확인**

```bash
cd /home/park/workspace/osai/p1 && uv run pytest tests/ -v 2>&1 | tail -3
```
Expected: 모두 pass

- [ ] **Step 4: Commit**

```bash
cd /home/park/workspace/osai
git add p1/src/data/builder.py
git commit -m "improve: builder.py에 Copy-Paste + Class-balanced sampling 통합

data.copy_paste.enabled, data.class_balanced flag 추가 (둘 다 Stage 2 한정).
조건부 wrapping/sampler — 기존 동작 영향 없음.

Regression test 통과."
```

---

## Phase C — Ablation 학습

### Task 11: v2.A (Copy-Paste) yaml + 학습 + eval

**Files:**
- Create: `p1/src/config/v2_A_copy_paste.yaml`

- [ ] **Step 1: yaml 작성**

`p1/src/config/v2_A_copy_paste.yaml`:

```yaml
# v2.A ablation: Stage 2 (VOC train) + Copy-Paste augmentation
# Bootstrap from v2.0 best ckpt
# Usage: uv run python -m src.train --config src/config/v2_A_copy_paste.yaml --stage 2

seed: 42
device: cuda

data:
  voc_root: ./data/voc
  coco_root: ./data/coco
  batch_size: 16
  num_workers: 8
  pin_memory: true
  persistent_workers: true
  crop_size: 480
  scale_range: [0.5, 2.0]
  stage: 2
  copy_paste:
    enabled: true
    p: 0.5
    num_paste: [1, 3]
  class_balanced: false

model:
  backbone: resnet50
  head: deeplabv3plus
  num_classes: 21
  output_stride: 16
  pretrained: true
  use_aux: true
  aspp_rates: [6, 12, 18]

loss:
  ignore_index: 255
  dice_weight: 0.5
  lovasz_weight: 0.0
  aux_weight: 0.4

optimizer:
  type: sgd
  base_lr: 0.001
  momentum: 0.9
  weight_decay: 0.0005
  backbone_lr_mult: 0.1

scheduler:
  type: poly
  power: 0.9
  warmup_iters: 500

training:
  stage1_iters: 80000           # not used for --stage 2
  stage2_iters: 8000
  stage2_base_lr: 0.001
  stage2_warmup: 500
  log_interval: 50
  val_interval_stage1: 5000
  val_interval_stage2: 1000
  ckpt_interval: 2000
  amp_dtype: fp16
  ema_decay: 0.9999
  grad_clip: 1.0

wandb:
  entity: g1nie-sungkyunkwan-university
  project: osai-p1-local
  run_name_prefix: v2.A
  tags: [desktop, voc-only, ablation, v2.A, copy-paste]

paths:
  bootstrap_from: ./checkpoints/v2.0/best.pth
  ckpt_dir: ./checkpoints/v2.A
  training_state: ./checkpoints/v2.A/training_state.pth
  best_ckpt: ./checkpoints/v2.A/best.pth
  final_ckpt: ./checkpoints/v2.A/model.pth
```

- [ ] **Step 2: yaml commit**

```bash
cd /home/park/workspace/osai
git add p1/src/config/v2_A_copy_paste.yaml
git commit -m "improve: v2.A yaml (Copy-Paste, Stage 2, bootstrap from v2.0)"
```

- [ ] **Step 3: smoke test (50 iter, batch 4)**

임시 yaml `/tmp/v2_A_smoke.yaml` 만들어서 단축 학습:
- `stage2_iters: 50`
- `batch_size: 4`
- `crop_size: 256`
- `val_interval_stage2: 50`
- `ckpt_interval: 25`
- `paths.ckpt_dir: /tmp/v2_A_smoke_ckpt`
- `paths.bootstrap_from: ./checkpoints/v2.0/best.pth`

```bash
cd /home/park/workspace/osai/p1
WANDB_MODE=offline uv run python -m src.train --config /tmp/v2_A_smoke.yaml --stage 2 2>&1 | tail -10
```
Expected: 50 iter 완료, `[copy-paste] pool size: ~5000-7000 instances`, val mIoU 출력

- [ ] **Step 4: 본격 v2.A 학습 (background, ~6h)**

```bash
cd /home/park/workspace/osai/p1
uv run python -m src.train --config src/config/v2_A_copy_paste.yaml --stage 2 &
```

ETA ~6h. 완료 알림 받으면 다음 step.

- [ ] **Step 5: v2.A eval**

```bash
cd /home/park/workspace/osai/p1
uv run python -m src.eval --config src/config/v2_A_copy_paste.yaml --ckpt checkpoints/v2.A/best.pth 2>&1 | tee /tmp/v2_A_eval.log
uv run python -m src.eval_tta --config src/config/v2_A_copy_paste.yaml --ckpt checkpoints/v2.A/best.pth 2>&1 | tee /tmp/v2_A_tta.log
```

결과 기록:

```bash
cd /home/park/workspace/osai
git commit --allow-empty -m "result: v2.A (Copy-Paste) measured

raw mIoU: $(grep '^mIoU' /tmp/v2_A_eval.log | head -1)
TTA mIoU: $(grep '^TTA mIoU' /tmp/v2_A_tta.log | head -1)
Δ vs v2.0: TBD
"
```

---

### Task 12: v2.B (Class-balanced) yaml + 학습 + eval

**Files:**
- Create: `p1/src/config/v2_B_class_balanced.yaml`

- [ ] **Step 1: yaml 작성**

v2.A yaml 복사 후 다음만 변경:
- `data.copy_paste.enabled: false`
- `data.class_balanced: true`
- `wandb.run_name_prefix: v2.B`
- `wandb.tags: [desktop, voc-only, ablation, v2.B, class-balanced]`
- `paths.ckpt_dir: ./checkpoints/v2.B`
- `paths.training_state: ./checkpoints/v2.B/training_state.pth`
- `paths.best_ckpt: ./checkpoints/v2.B/best.pth`
- `paths.final_ckpt: ./checkpoints/v2.B/model.pth`

```yaml
# v2.B ablation: Stage 2 + Class-balanced sampling
# (full yaml — 위 v2.A 양식에서 변경된 부분 적용)
seed: 42
device: cuda

data:
  voc_root: ./data/voc
  coco_root: ./data/coco
  batch_size: 16
  num_workers: 8
  pin_memory: true
  persistent_workers: true
  crop_size: 480
  scale_range: [0.5, 2.0]
  stage: 2
  copy_paste:
    enabled: false
  class_balanced: true

model:
  backbone: resnet50
  head: deeplabv3plus
  num_classes: 21
  output_stride: 16
  pretrained: true
  use_aux: true
  aspp_rates: [6, 12, 18]

loss:
  ignore_index: 255
  dice_weight: 0.5
  lovasz_weight: 0.0
  aux_weight: 0.4

optimizer:
  type: sgd
  base_lr: 0.001
  momentum: 0.9
  weight_decay: 0.0005
  backbone_lr_mult: 0.1

scheduler:
  type: poly
  power: 0.9
  warmup_iters: 500

training:
  stage1_iters: 80000
  stage2_iters: 8000
  stage2_base_lr: 0.001
  stage2_warmup: 500
  log_interval: 50
  val_interval_stage1: 5000
  val_interval_stage2: 1000
  ckpt_interval: 2000
  amp_dtype: fp16
  ema_decay: 0.9999
  grad_clip: 1.0

wandb:
  entity: g1nie-sungkyunkwan-university
  project: osai-p1-local
  run_name_prefix: v2.B
  tags: [desktop, voc-only, ablation, v2.B, class-balanced]

paths:
  bootstrap_from: ./checkpoints/v2.0/best.pth
  ckpt_dir: ./checkpoints/v2.B
  training_state: ./checkpoints/v2.B/training_state.pth
  best_ckpt: ./checkpoints/v2.B/best.pth
  final_ckpt: ./checkpoints/v2.B/model.pth
```

- [ ] **Step 2: 학습 (background, ~6h) + eval**

```bash
cd /home/park/workspace/osai
git add p1/src/config/v2_B_class_balanced.yaml
git commit -m "improve: v2.B yaml (class-balanced sampling, Stage 2)"

cd p1
uv run python -m src.train --config src/config/v2_B_class_balanced.yaml --stage 2 &
```

완료 알림 후:
```bash
cd /home/park/workspace/osai/p1
uv run python -m src.eval --config src/config/v2_B_class_balanced.yaml --ckpt checkpoints/v2.B/best.pth 2>&1 | tee /tmp/v2_B_eval.log
uv run python -m src.eval_tta --config src/config/v2_B_class_balanced.yaml --ckpt checkpoints/v2.B/best.pth 2>&1 | tee /tmp/v2_B_tta.log
```

결과 기록 (empty commit).

---

### Task 13: v2.C (Lovász) yaml + 학습 + eval

**Files:**
- Create: `p1/src/config/v2_C_lovasz.yaml`

- [ ] **Step 1: yaml 작성**

v2.B yaml 복사 후 변경:
- `data.copy_paste.enabled: false`, `data.class_balanced: false` (default)
- `loss.dice_weight: 0.0` (Dice 끔)
- `loss.lovasz_weight: 0.5` (Lovász on)
- `wandb.run_name_prefix: v2.C`
- `wandb.tags: [desktop, voc-only, ablation, v2.C, lovasz]`
- `paths.ckpt_dir: ./checkpoints/v2.C` (등)

```yaml
# v2.C ablation: Stage 2 + Lovász loss (Dice 교체)
seed: 42
device: cuda

data:
  voc_root: ./data/voc
  coco_root: ./data/coco
  batch_size: 16
  num_workers: 8
  pin_memory: true
  persistent_workers: true
  crop_size: 480
  scale_range: [0.5, 2.0]
  stage: 2
  copy_paste:
    enabled: false
  class_balanced: false

model:
  backbone: resnet50
  head: deeplabv3plus
  num_classes: 21
  output_stride: 16
  pretrained: true
  use_aux: true
  aspp_rates: [6, 12, 18]

loss:
  ignore_index: 255
  dice_weight: 0.0          # ★ disabled
  lovasz_weight: 0.5        # ★ enabled
  aux_weight: 0.4

optimizer:
  type: sgd
  base_lr: 0.001
  momentum: 0.9
  weight_decay: 0.0005
  backbone_lr_mult: 0.1

scheduler:
  type: poly
  power: 0.9
  warmup_iters: 500

training:
  stage1_iters: 80000
  stage2_iters: 8000
  stage2_base_lr: 0.001
  stage2_warmup: 500
  log_interval: 50
  val_interval_stage1: 5000
  val_interval_stage2: 1000
  ckpt_interval: 2000
  amp_dtype: fp16
  ema_decay: 0.9999
  grad_clip: 1.0

wandb:
  entity: g1nie-sungkyunkwan-university
  project: osai-p1-local
  run_name_prefix: v2.C
  tags: [desktop, voc-only, ablation, v2.C, lovasz]

paths:
  bootstrap_from: ./checkpoints/v2.0/best.pth
  ckpt_dir: ./checkpoints/v2.C
  training_state: ./checkpoints/v2.C/training_state.pth
  best_ckpt: ./checkpoints/v2.C/best.pth
  final_ckpt: ./checkpoints/v2.C/model.pth
```

- [ ] **Step 2: 학습 (background, ~7-8h Lovász overhead) + eval**

```bash
cd /home/park/workspace/osai
git add p1/src/config/v2_C_lovasz.yaml
git commit -m "improve: v2.C yaml (Lovász loss, Dice 교체, Stage 2)"

cd p1
uv run python -m src.train --config src/config/v2_C_lovasz.yaml --stage 2 &
```

완료 알림 후:
```bash
cd /home/park/workspace/osai/p1
uv run python -m src.eval --config src/config/v2_C_lovasz.yaml --ckpt checkpoints/v2.C/best.pth 2>&1 | tee /tmp/v2_C_eval.log
uv run python -m src.eval_tta --config src/config/v2_C_lovasz.yaml --ckpt checkpoints/v2.C/best.pth 2>&1 | tee /tmp/v2_C_tta.log
```

결과 기록.

---

## Phase D — 결과 분석 + v2.final

### Task 14: 비교 표 생성 + v2.final 결정

**Files:**
- Create: `p1/src/compare_runs.py`
- Create: `p1/src/config/v2_final.yaml`

- [ ] **Step 1: compare_runs.py 작성**

```python
"""Read all ablation ckpts and print comparison table."""
from __future__ import annotations

import argparse
from pathlib import Path

import torch


def get_ckpt_miou(path: str) -> float:
    if not Path(path).exists():
        return float("nan")
    ckpt = torch.load(path, map_location="cpu", weights_only=False)
    return float(ckpt.get("miou", float("nan")))


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--ckpt-root", default="./checkpoints")
    args = parser.parse_args()

    root = Path(args.ckpt_root)
    variants = ["v2.0", "v2.A", "v2.B", "v2.C", "v2.final"]

    print(f"{'Variant':<10} {'best.pth mIoU':<15}")
    print("-" * 30)
    for v in variants:
        miou = get_ckpt_miou(str(root / v / "best.pth"))
        print(f"{v:<10} {miou:<15.4f}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: compare_runs.py commit + 실행**

```bash
cd /home/park/workspace/osai
git add p1/src/compare_runs.py
git commit -m "improve: add compare_runs.py — print mIoU per variant ckpt"

cd p1
uv run python -m src.compare_runs
```

Output 예시:
```
Variant    best.pth mIoU
------------------------------
v2.0       0.7650
v2.A       0.7820   ← Δ = +1.7%
v2.B       0.7710   ← Δ = +0.6%
v2.C       0.7720   ← Δ = +0.7%
```

- [ ] **Step 3: TTA mIoU 결과 종합 (수동)**

각 `/tmp/v2_*_tta.log` 결과 종합. 다음 표 작성 (commit message에 또는 docs/ 신규 파일에):

| Variant | raw mIoU | TTA mIoU | Δ TTA vs v2.0 | Decision |
|---|---|---|---|---|
| v2.0 | 0.XX | 0.XX | 0 | base |
| v2.A | 0.XX | 0.XX | +X.X% | keep if > +1.0 |
| v2.B | 0.XX | 0.XX | +X.X% | keep if > +0.3 |
| v2.C | 0.XX | 0.XX | +X.X% | keep if > +0.5 |

per-ablation threshold (spec § 8.2) 적용.

- [ ] **Step 4: v2.final yaml 작성 (winning combination 반영)**

`p1/src/config/v2_final.yaml`:

```yaml
# v2.final: winning ablation 조합 + Stage 1+2 통째 재학습
# 아래 *_ENABLED 부분은 ablation 결과에 따라 true/false로 채움
# (예: A=true, B=true, C=false → Copy-Paste + Class-balanced만)

seed: 42
device: cuda

data:
  voc_root: ./data/voc
  coco_root: ./data/coco
  batch_size: 16
  num_workers: 8
  pin_memory: true
  persistent_workers: true
  crop_size: 480
  scale_range: [0.5, 2.0]
  stage: 1
  copy_paste:
    enabled: <COPY_PASTE_ENABLED>     # ablation 결과에 따라 true/false
    p: 0.5
    num_paste: [1, 3]
  class_balanced: <CLASS_BALANCED_ENABLED>  # true/false

model:
  backbone: resnet50
  head: deeplabv3plus
  num_classes: 21
  output_stride: 16
  pretrained: true
  use_aux: true
  aspp_rates: [6, 12, 18]

loss:
  ignore_index: 255
  dice_weight: <DICE_WEIGHT>         # 0.5 if Lovász not selected, 0.0 if Lovász selected
  lovasz_weight: <LOVASZ_WEIGHT>     # 0.5 if Lovász selected, 0.0 otherwise
  aux_weight: 0.4

optimizer:
  type: sgd
  base_lr: 0.01
  momentum: 0.9
  weight_decay: 0.0005
  backbone_lr_mult: 0.1

scheduler:
  type: poly
  power: 0.9
  warmup_iters: 1000

training:
  stage1_iters: 160000     # v2.0과 동일 (Stage 1 길게)
  stage2_iters: 8000
  stage2_base_lr: 0.001
  stage2_warmup: 500
  log_interval: 50
  val_interval_stage1: 5000
  val_interval_stage2: 1000
  ckpt_interval: 5000
  amp_dtype: fp16
  ema_decay: 0.9999
  grad_clip: 1.0

wandb:
  entity: g1nie-sungkyunkwan-university
  project: osai-p1-local
  run_name_prefix: v2.final
  tags: [desktop, voc+coco, ablation, v2.final, combined]

paths:
  ckpt_dir: ./checkpoints/v2.final
  training_state: ./checkpoints/v2.final/training_state.pth
  best_ckpt: ./checkpoints/v2.final/best.pth
  final_ckpt: ./checkpoints/v2.final/model.pth
```

placeholder `<COPY_PASTE_ENABLED>` 등을 실제 결정값으로 치환:
- v2.A 결과 + 기준 만족하면 → `true`, 아니면 `false`
- v2.B 결과 + 기준 만족하면 → `true`, 아니면 `false`
- v2.C 결과 + 기준 만족하면 → `dice_weight: 0.0, lovasz_weight: 0.5`, 아니면 `dice_weight: 0.5, lovasz_weight: 0.0`

수동 sed 또는 직접 편집.

- [ ] **Step 5: v2.final yaml commit**

```bash
cd /home/park/workspace/osai
git add p1/src/config/v2_final.yaml
git commit -m "improve: v2.final yaml — combined config based on ablation results

Winning ablations: <list>
Stage 1 160K + Stage 2 8K with selected ablations enabled."
```

---

### Task 15: v2.final 학습 + eval

**Files:** (verification only)

- [ ] **Step 1: v2.final 학습 (background, ~11.5h)**

```bash
cd /home/park/workspace/osai/p1
uv run python -m src.train --config src/config/v2_final.yaml &
```

Stage 1 + Stage 2 자동. ETA ~11.5h.

- [ ] **Step 2: v2.final eval (raw + TTA)**

```bash
cd /home/park/workspace/osai/p1
uv run python -m src.eval --config src/config/v2_final.yaml --ckpt checkpoints/v2.final/best.pth 2>&1 | tee /tmp/v2_final_eval.log
uv run python -m src.eval_tta --config src/config/v2_final.yaml --ckpt checkpoints/v2.final/best.pth 2>&1 | tee /tmp/v2_final_tta.log
```

- [ ] **Step 3: 최종 비교 표 정리**

```bash
cd /home/park/workspace/osai/p1
uv run python -m src.compare_runs
```

전체 ablation history를 문서로:

```bash
cd /home/park/workspace/osai
git commit --allow-empty -m "result: v2.final measured

raw mIoU: $(grep '^mIoU' /tmp/v2_final_eval.log | head -1)
TTA mIoU: $(grep '^TTA mIoU' /tmp/v2_final_tta.log | head -1)

Final ablation summary:
  v1 (75.82 raw / 79.47 TTA)
  v2.0 → ?
  v2.A → ?
  v2.B → ?
  v2.C → ?
  v2.final → ?

Best variant for Colab repro: <choose v2.final or best individual>
"
```

- [ ] **Step 4: 다음 단계 결정**

- v2.final이 best → Colab 재현 시 v2.final config 사용
- v2.A/B/C 중 하나가 best → 그 yaml로 Colab 재현
- 모두 v2.0보다 안 좋음 → v2.0 config로 Colab (또는 v1 config)

이 결정을 commit message에 명시.

---

## Self-Review

### 1. Spec 커버리지

| Spec § | Plan task |
|---|---|
| §3 Hybrid 구조 | Task 1 (v2.0), Tasks 11-13 (A/B/C), Task 15 (final) |
| §4 v2.0 Stage 1 160K | Task 1 |
| §5 Copy-Paste algorithm | Task 8 (instance pool), Task 9 (paste + dataset) |
| §5 yaml option | Task 11 |
| §6 Class-balanced sampling | Task 2 (sampler.py) |
| §6 yaml option | Task 12 |
| §7 Lovász loss algorithm | Task 3 (lovasz.py) |
| §7 SegLoss 결합 | Task 4 |
| §7 yaml option | Task 13 |
| §8 Decision tree + per-ablation threshold | Task 14 (compare + decision) |
| §8 v2.final 학습 전략 | Task 15 |
| §9 Reporting (비교 표) | Task 14 (compare_runs.py) |
| §10 Workflow phases | Phase A/B/C/D 구조로 매핑 |
| §11 Risk management | 각 task의 smoke test + commit/rollback |
| §12 Stop conditions | Task 14 (decision logic) |
| Bootstrap mechanism | Task 5 |
| VOCSegDataset refactor | Task 7 |
| builder.py 통합 | Task 10 |

**누락 없음.**

### 2. Placeholder scan

- 결과값(?, mIoU 0.XX, etc.)은 실제 측정 후 채워질 자리 — placeholder 아님 (의도)
- v2.final yaml의 `<COPY_PASTE_ENABLED>` 등은 ablation 결과에 따라 결정되는 변수 — Task 14에서 명시적으로 치환 단계 있음
- TBD/TODO 없음
- "implement later" 없음

### 3. Type/method 일관성

- `lovasz_softmax(logits, labels, ignore_index=255)` Task 3에서 정의, Task 4에서 호출 — 일치
- `compute_class_pixel_counts`, `compute_image_weights`, `build_balanced_sampler` Task 2에서 정의, Task 10에서 호출 — 일치
- `build_instance_pool`, `paste_instance`, `CopyPasteDataset` Task 8/9에서 정의, Task 10에서 호출 — 일치
- `VOCSegDataset.get_raw`, `apply_transform` Task 7에서 추가, Task 9에서 호출 — 일치
- `paths.bootstrap_from` Task 5에서 추가, Tasks 11/12/13 yaml에서 사용 — 일치

**일관성 OK.**

---

## Execution Choice

Plan complete and saved to `docs/superpowers/plans/2026-04-25-p1-improve.md`. Two execution options:

**1. Subagent-Driven (recommended)** — fresh subagent per task, review between tasks, fast iteration. agent-team-design.md의 data-augmentation-engineer (Tasks 7-10) + loss-designer (Tasks 3-4) + training-strategist (Tasks 1, 5, 11-15) 매핑 가능.

**2. Inline Execution** — execute tasks in this session using executing-plans, batch execution with checkpoints.

Which approach?
